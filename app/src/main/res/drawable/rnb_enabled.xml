<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- background -->
    <solid android:color="@color/colorAccent" />
    <padding
        android:bottom="0dp"
        android:left="0dp"
        android:right="0dp"
        android:top="0dp" />
    <size
        android:width="64dp"
        android:height="64dp" />

    <!-- border -->
    <stroke
        android:width="3dp"
        android:color="@color/navigator_border_color" />
</shape>